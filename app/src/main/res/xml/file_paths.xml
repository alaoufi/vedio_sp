<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exported backups (shared via an explicit user action). -->
    <files-path name="backups" path="backups/" />
    <!-- Downloaded/imported videos, for the per-video Share action. -->
    <files-path name="videos" path="videos/" />
    <files-path name="downloads" path="downloads/" />
</paths>
