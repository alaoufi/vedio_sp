<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network access for provider search / downloads (used in later phases). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Post notifications (Android 13+) for download progress. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service for background downloads. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Media access. Scoped per SDK level. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".MyVideoLibraryApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyVideoLibrary"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">

        <!-- ===== Activities ===== -->

        <!-- Launcher gate: PIN / biometric lock, then routes to the library. -->
        <activity
            android:name=".ui.security.LockActivity"
            android:exported="true"
            android:theme="@style/Theme.MyVideoLibrary">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Offline license activation gate. -->
        <activity
            android:name=".ui.security.ActivationActivity"
            android:exported="false"
            android:theme="@style/Theme.MyVideoLibrary" />

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.MyVideoLibrary" />

        <!-- Import videos from device storage (MediaStore). -->
        <activity
            android:name=".ui.importer.ImportActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Search providers (YouTube) and download. -->
        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Add a download from a pasted or shared TikTok/YouTube link. -->
        <activity
            android:name=".ui.provider.AddDownloadActivity"
            android:exported="true"
            android:parentActivityName=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Download management. -->
        <activity
            android:name=".ui.downloads.DownloadsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.categories.CategoriesActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Player: supports PiP, rotation, and keeps screen on. -->
        <activity
            android:name=".ui.player.PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode"
            android:supportsPictureInPicture="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.MyVideoLibrary.Player" />

        <!-- FileProvider for sharing exported backups. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Use on-demand WorkManager init (via the app's Configuration.Provider +
             HiltWorkerFactory). Removing the default initializer avoids the
             androidx.startup ContentProvider running before Hilt injects the factory. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
